<%= form_for(mooc) do |f| %>
  <% if mooc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mooc.errors.count, "error") %> prohibited this mooc from being saved:</h2>

      <ul>
      <% mooc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :institute %>
    <%= f.text_area :institute %>
  </div>

  <div class="field">
    <%= f.label :course %>
    <%= f.text_area :course %>
  </div>

  <div class="field">
    <%= f.label :year %>
    <%= f.text_field :year %>
  </div>

  <div class="field">
    <%= f.label :verified %>
    <%= f.check_box :verified %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
